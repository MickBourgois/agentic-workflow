{
  "name": "agentic-workflow",
  "version": "0.2.0",
  "private": true,
  "description": "Portable, risk-adaptive agentic coding workflow for Codex and Claude Code",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "smoke": "node scripts/smoke-test.mjs",
    "scan:secrets": "node scripts/scan-secrets.mjs",
    "check": "npm run test && npm run smoke && npm run scan:secrets && npm run site:test && npm run site:build",
    "site:build": "node website/build.mjs",
    "site:dev": "node website/server.mjs",
    "site:test": "node --test website/tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
